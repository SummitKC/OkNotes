<UserControl x:Class="TwoOkNotes.Views.PenSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TwoOkNotes.ViewModels" 
             xmlns:model="clr-namespace:TwoOkNotes.Model" 
             mc:Ignorable="d" 
             d:DesignHeight="259.119" d:DesignWidth="258.709">
    <Grid Background="Aqua">
        <StackPanel>
         

            <!-- Individual sliders for RGB -->
            <TextBlock Text="Red" Margin="10" Height="12"/>
            <Slider Minimum="0" Maximum="255" Value="{Binding Red, Mode=TwoWay}"/>

            <TextBlock Text="Green" Margin="10" Height="11"/>
            <Slider Minimum="0" Maximum="255" Value="{Binding Green, Mode=TwoWay}"/>

            <TextBlock Text="Blue" Margin="10"/>
            <Slider Minimum="0" Maximum="255" Value="{Binding Blue, Mode=TwoWay}"/>

            <TextBlock Text="Pen Thickness" Margin="10"/>
            <Slider Minimum="1" Maximum="50" Value="{Binding ThickNess, Mode=TwoWay}"/>

            <TextBlock Text="Pen Opacity" Margin="10"/>
            <Slider Minimum="0" Maximum="255" Value="{Binding Opacity, Mode=TwoWay}"/>
        </StackPanel>
    </Grid>
</UserControl>
